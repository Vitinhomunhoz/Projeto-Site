# Projeto Site Full-Stack (React + Node.js)

Bem-vindo ao Projeto Site! Esta é uma aplicação web full-stack desenvolvida com um frontend moderno em React e um backend robusto utilizando Node.js, Express e MongoDB. O objetivo principal da aplicação é gerenciar usuários, permitindo o cadastro e a consulta de informações.

---

## 📜 Índice

* [Tecnologias Utilizadas](#-tecnologias-utilizadas)
* [Pré-requisitos](#-pré-requisitos)
* [Como Executar o Projeto](#-como-executar-o-projeto)
    * [Backend](#-backend)
    * [Frontend](#-frontend)
* [Estrutura de Pastas](#-estrutura-de-pastas)
* [Endpoints da API](#-endpoints-da-api)
* [Próximos Passos](#-próximos-passos)

---

## ✨ Tecnologias Utilizadas

Este projeto foi construído utilizando as seguintes tecnologias:

### **Frontend**
* **[React](https://reactjs.org/)**: Biblioteca para construir interfaces de usuário.
* **[TypeScript](https://www.typescriptlang.org/)**: Superset do JavaScript que adiciona tipagem estática.
* **[Vite](https://vitejs.dev/)**: Ferramenta de build moderna e ultrarrápida para o desenvolvimento frontend.

### **Backend**
* **[Node.js](https://nodejs.org/)**: Ambiente de execução JavaScript no servidor.
* **[Express](https://expressjs.com/)**: Framework para Node.js que simplifica a criação de APIs.
* **[MongoDB](https://www.mongodb.com/)**: Banco de dados NoSQL orientado a documentos.
* **[Mongoose](https://mongoosejs.com/)**: Biblioteca para modelagem de objetos MongoDB em Node.js.
* **[Dotenv](https://github.com/motdotla/dotenv)**: Para gerenciar variáveis de ambiente.
* **[Nodemon](https://nodemon.io/)**: Ferramenta que reinicia o servidor automaticamente durante o desenvolvimento.

---

## 🔧 Pré-requisitos

Antes de começar, certifique-se de que você tem as seguintes ferramentas instaladas em sua máquina:
* [Node.js](https://nodejs.org/en/) (versão 14 ou superior)
* [npm](https://www.npmjs.com/) (geralmente vem com o Node.js)
* [MongoDB](https://www.mongodb.com/try/download/community) (instância local ou uma conta no [MongoDB Atlas](https://www.mongodb.com/cloud/atlas))

---

## 🚀 Como Executar o Projeto

Siga os passos abaixo para rodar a aplicação em seu ambiente local.

### 🔌 Backend

1.  **Navegue até a pasta do backend:**
    ```bash
    cd backend
    ```

2.  **Instale as dependências:**
    ```bash
    npm install
    ```

3.  **Configure as variáveis de ambiente:**
    * Crie um arquivo `.env` na raiz da pasta `backend`.
    * Copie o conteúdo do exemplo abaixo e cole no seu arquivo `.env`, substituindo os valores conforme necessário.

    **.env.example**
    ```ini
    # Porta em que a API será executada
    PORT=3001

    # Sua string de conexão com o MongoDB
    MONGO_URI="mongodb://localhost:27017/projeto-site"
    ```

4.  **Inicie o servidor de desenvolvimento:**
    ```bash
    npm run dev
    ```
    * O servidor estará rodando em `http://localhost:3001`.

### 🖥️ Frontend

**Importante:** Mantenha o terminal do backend rodando e abra um **novo terminal** para o frontend.

1.  **Navegue até a pasta do frontend:**
    ```bash
    cd frontend
    ```

2.  **Instale as dependências:**
    ```bash
    npm install
    ```

3.  **Inicie o servidor de desenvolvimento:**
    ```bash
    npm run dev
    ```
    * A aplicação React estará disponível em `http://localhost:5173` (ou outra porta indicada pelo Vite).

---

## 📁 Estrutura de Pastas

O projeto é dividido em duas pastas principais:

* **`/backend`**: Contém toda a lógica do servidor, incluindo a API, conexão com o banco de dados e modelos.
    * `/config`: Arquivos de configuração (ex: conexão com DB).
    * `/controllers`: Funções que executam a lógica de negócio para cada rota.
    * `/models`: Schemas do Mongoose que definem a estrutura dos dados.
    * `/routes`: Definição dos endpoints da API.
* **`/frontend`**: Contém a aplicação cliente desenvolvida em React.
    * `/src`: Código-fonte da aplicação.
        * `/components`: Componentes reutilizáveis.
        * `/pages`: Páginas da aplicação.
        * `/services`: Funções para se comunicar com a API.

---

## 📡 Endpoints da API

A API atualmente possui os seguintes endpoints para o recurso de usuários:

| Método | Rota               | Descrição                        |
| :----- | :----------------- | :--------------------------------- |
| `POST` | `/api/users`       | Cria um novo usuário.              |
| `GET`  | `/api/users`       | Lista todos os usuários.           |
| `GET`  | `/api/users/:id`   | Busca um usuário específico por ID.|

**Exemplo de corpo para `POST /api/users`:**
```json
{
  "name": "Nome do Usuário",
  "email": "usuario@email.com",
  "password": "senhaSegura123"
}